‘’’    
Windows web server setup 
1)create a instance on AWS >> windows >> t3 medium >> connect via RDP using DNS >> password and username  
  1.1) decrpt key pair >> extension .pem >>
  1.2) security rules > inbound >> RDP rule 3389 >> http 80 >> htttps 443

2)installation >> server manager >> add roles >> iis >> next > install 
3) c drive >> intpub folder >> wwwroot  >> index.html or any page     

‘’’
